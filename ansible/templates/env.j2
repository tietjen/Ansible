# Docker Compose environment configuration for {{ inventory_hostname }}
# Generated by Ansible

# Application settings
APP_ENV=production
APP_DEBUG=false
APP_URL=http://{{ inventory_hostname }}

# Database settings (if needed)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=app
DB_USER=app_user
DB_PASSWORD=change_me

# Redis settings (if needed)
REDIS_HOST=localhost
REDIS_PORT=6379

# Logging
LOG_LEVEL=info
LOG_CHANNEL=stack 