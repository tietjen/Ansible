# System limits configuration for {{ inventory_hostname }}
# Generated by Ansible

# Core dumps
* soft core 0
* hard core 0

# File descriptors
* soft nofile 65536
* hard nofile 65536

# Stack size
* soft stack 8192
* hard stack 8192

# Memory limits
* soft memlock unlimited
* hard memlock unlimited

# Process limits
* soft nproc 32768
* hard nproc 32768

# User specific limits
{{ ansible_user }} soft nofile 65536
{{ ansible_user }} hard nofile 65536
{{ ansible_user }} soft nproc 32768
{{ ansible_user }} hard nproc 32768 